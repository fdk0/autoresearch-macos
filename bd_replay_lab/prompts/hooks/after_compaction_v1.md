# Hook after_compaction v1

After compaction:

- restore lane identity
- restore next required helper reads
- restore any active continuity/freshness warning
- avoid re-injecting unrelated workflow prose
